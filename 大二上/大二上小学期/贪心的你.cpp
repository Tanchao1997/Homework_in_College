#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#define len 10005
struct ty
{
	int score, limit;
}que[len];
int cmp(const void*a, const  void*b)
{
	return *(int*)a>*(int*)b; 
}
int main()
{
	
}
